// Description:
//    tests de déclaration de booléen
//
// Resultats:
//    rien

{
   int x = 2;
   int y = 4;
   boolean bool;
   bool = (x==x) == (y==y);
   bool = ((x < y) != (x <= y) == (x > x) == (y>=y)) != ((x>=y) == (y <x) != (x<= x));
	!bool;
}

