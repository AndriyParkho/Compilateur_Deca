//Description:
//  Vérification contextuelle du type de sortie non int de >=. Si le résultat générait un string il serait affichable.
//
//Résultat:
//  Erreur contextuelle
//  ligne 8 : Le type boolean n'est pas affichable (les types affichables sont int,float et string)

{
   println(1>=0);
}